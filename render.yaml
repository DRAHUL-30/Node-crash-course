services:
  - name: frontend
    type: web
    buildCommand: npm run build
    startCommand: npm start
    root: ./

  - name: Backend
    type: web
    buildCommand: npm run build
    startCommand: npm start
    root: ./
